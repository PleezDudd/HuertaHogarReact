require("jasmine-expect");
require("@testing-library/jasmine-dom");

// Configuración global para las pruebas
beforeEach(() => {
  // Configuración previa a cada test
});

afterEach(() => {
  // Limpieza posterior a cada test
});
